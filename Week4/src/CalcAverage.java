import java.util.Random;
public class CalcAverage {
    public static void main(String[] args) {


    }
    /*public int makeArray(int n) { //생성자 아님. 생성자는 클래스 명과 같아야 함 이름이 같은 메서드

        int[] data = new int[n];
        Random rand = new Random(101); // 매개변수가 1개이면 실행 , rand 클래스의 객체에 대한 참조값
        for (int i = 0; i < data.length; i++) {
            data[i] = rand.nextInt(101);

        }
        return data;
    }


    public int makeArray(int n, int low, int high) //매개변수가 3개이면 실행되는 메서드. 오버로딩
    {
        return null;
    }

    public int getTotal(int[] data){ //정수값을 여러개 저장된 배열 객체의 참조값. 데이터를 꺼내와 합을 구하기
        int total = 0;
        for (int i = 0; i < data.length; i++) {
            total += data[i];

        }
        return data;

    }
    public double getEverage(int[] data){
        int total = getTotal(data);
        double avg = (double)total / data.length;
        return avg;

    }
}

*/
}